# Namespaced project name that the target belongs to
project: pulsarctl

# Target name (should be unique within the project)
target: functions-create

# List of commands used to test the target
cmds:

    # Command used to start the target, "@@" is the input file
    # (when "@@" is omitted Mayhem defaults to stdin inputs)
    - cmd: ./pulsarctl functions create --function-config-file @@
#    - cmd: ./pulsarctl functions update --function-config-file @@
#    - cmd: ./pulsarctl sinks create --sink-config-file @@
#    - cmd: ./pulsarctl sinks update --sink-config-file @@
#    - cmd: ./pulsarctl sources update --source-config-file @@
#    - cmd: ./pulsarctl token show --token-file @@
#    - cmd: ./pulsarctl token validate --token-string "mayhem" --public-key-file @@